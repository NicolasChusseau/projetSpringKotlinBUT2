spring:
  devtools:
    restart:
      enabled: 'true'
      poll-interval: '2s'
      quiet-period: '1s'
      additional-paths:
        - src/main/kotlin/

management:
  server.port: 8082
  endpoints:
    web:
      exposure:
        include: info, health
